[phases.setup]
nixPkgs = ["nodejs_20", "yarn"]

[phases.install]
cmds = ["yarn install --frozen-lockfile"]

[phases.build]
cmds = ["yarn build"]

[phases.deploy]
cmds = ["yarn migration:run", "node dist/main"]

[variables]
NODE_ENV = "production"
